#include "usbpd.h"

void __attribute__((weak)) usbpd_init() 
{
    // Initialize the USB PD stack
}

void __attribute__((weak)) usbpd_wait_for_power_ready() 
{
    // Wait for the USB PD stack to signal that power is ready
}